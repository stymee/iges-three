import * as THREE from 'three'
import { IgesData } from "../iges/standard";


export const createGeometry = (iges: IgesData) => {
	const pointGroup = new THREE.Group();

	iges.parameters.filter(s => s.values[0] === ).forEach()




}